# Redesign Progress

Tracks per-route-group status against `docs/design/SCREEN-WORKFLOW.md` and the approved execution plan. Status ladder per route: `Not Started` → `Mapped` → `Discovered` → `Baseline Captured` → `Implemented` → `Technical Verified` → `Browser Verified` → `Visual Verified` → `Done`.

## Phase 0 — Repository / Git Safety

- Branch: `ui-revamp` (confirmed current, up to date with `origin/ui-revamp`) — Hard Gate 1 clear.
- Stitch MCP: Connected.
- Playwright MCP: Connected.
- Dev server: Running locally (`npm run dev`, verified reachable).
- Status: **Done**.

## Phase 1 — Design System Extraction (global, one-time milestone)

- Extracted tokens from 7 representative Stitch screens (Login L/D, Settings L/D, Websites L/D, Execution Logs L) — see `docs/design/DESIGN-SYSTEM.md` for full reconciliation notes (Stitch screens used inconsistent per-screen naming; reconciled into one canonical scale).
- Wired into `app/globals.css` (`:root` and `.dark` CSS variables): blue-600 primary, slate-50/slate-900 light background/foreground, reconciled dark scale (`#070b14` bg → `#0f172a` card), rose-600 destructive. `tailwind.config.ts` already exposed the right semantic names (shadcn-style) — no structural change needed there.
- Extended existing primitives to match extracted radius data (`rounded-lg` dominant, 222 vs 83 occurrences for `rounded-md`): `components/ui/button.tsx`, `components/ui/input.tsx` changed `rounded-md` → `rounded-lg`. `components/ui/badge.tsx` success variant changed `green-*` → `emerald-*` (matches extracted success token). `components/ui/label.tsx` unchanged (no issues found).
- Sanity-verified via Playwright at 1440×900 on the *current* (not-yet-redesigned) `/login` route: new tokens render cleanly, no console errors, no visual breakage. Confirmed the current Login markup doesn't yet respond to `.dark` (hardcoded classes, not semantic tokens) — expected, to be fixed in Phase 2, not a Phase 1 regression.
- Explicitly deferred (not built): `Card`, `PageHeader`, `PageContainer`/`AppShell`, `ThemeToggle`, `DataTable`, `EmptyState`, `LoadingState`, `FormField`, `ConfirmDialog`, `SearchInput`, `FilterBar`, charts — built when a route group first demonstrates real need (Dashboard Shell/Websites are next, so most of these arrive there).
- Status: **Done**.

### Stitch project mapping audit (informs Phases 2–17)

- Clean Light/Dark pairs confirmed for: Login, Websites, Lastmod Updater, Sitemap Cleaner, Backlink Sites, Backlinks Tracker, Weekly Reports, Daily Reports, Website Audit, Execution Logs, User Management (+ bonus "Invite Team Member" modal state), Settings.
- **Hard Gate 2 resolved** (Phase 5): Scripts workflow had 7 candidate screens — user selected Light `94551d91...`/Dark `8aef89a4...`.
- **Gap — Dashboard Shell (`/`)**: resolved (Phase 3) — generated Light/Dark designs in Stitch.
- **Gap — Content (`/content`)**: still open. Implemented (Phase 10) per user decision using `DESIGN.md`/established-screen consistency instead of a Stitch reference — **Stitch design generation for this route remains deferred**, to revisit later if a literal Stitch comparison is still wanted.
- **Gap — Indexing Queue (`/indexing-queue`)**: resolved (Phase 11) — per user decision, generated fresh Light/Dark designs in Stitch for both list and detail states (opposite of the Content approach).
- **Correction applied** (Phase 2): `screens/login.md`'s Dark ID typo (`...44eaad...` → `...45eaad...`) fixed.

## Route groups (Phases 2–17)

| # | Route group | Route(s) | Doc file | Status | Notes |
|---|---|---|---|---|---|
| 2 | Login | `/login` | `screens/login.md` | Done | Implemented, technical-verified (tsc/lint/build clean), browser-verified (3 breakpoints × Light/Dark, functional tests pass), Stitch-compared (P2/P3 only, no P0/P1). Pre-existing `tests/e2e/login.spec.ts` `getByLabel` failures confirmed unrelated (same failure on unmodified code) — reported, not fixed. |
| 3 | Dashboard Shell | `/` | `screens/dashboard-home.md` | Done | Stitch designs generated (no pre-existing reference existed). Implemented: restyled Sidebar (persistent dark navy, semantic tokens) + added mobile drawer (fixed pre-existing responsive defect), wired up previously-unused Topbar with ThemeToggle, restyled real KPI/activity data with Stitch visual language (no fictional content used). tsc/lint/build clean. Verified 3 breakpoints × Light/Dark via Playwright, 0 console errors. Fixed a real hydration bug (`suppressHydrationWarning`) and a Radix a11y warning (`SheetDescription`) discovered during verification. `tests/e2e/dashboard-nav.spec.ts` skipped (pre-existing: needs `E2E_TEST_EMAIL`/`E2E_TEST_PASSWORD`, not configured) — manually verified equivalent coverage. |
| 4 | Websites | `/websites` | `screens/websites.md` | Done | Restyled table in place (first table screen — `DataTable` extraction deferred until a 2nd/3rd screen repeats the pattern). Fixed 2 real pre-existing defects: `overflow-hidden` clipped Assigned Members/Actions columns entirely on mobile (now `overflow-x-auto`, verified scrollable), and row actions were hover-only (invisible to keyboard/touch — now always visible). Added `DialogDescription` to the shared `ui/dialog.tsx` primitive (was missing app-wide) and wired it into all 5 dialogs here. tsc/lint/build clean, 0 console errors/warnings across all verification. All 5 dialogs functionally tested (Add, Automation w/ real GSC account + sitemap count) with real data. |
| 5 | Scripts workflow | `/scripts`, `/scripts/[slug]` | `screens/scripts.md` | Done | Hard Gate 2 resolved (user picked Light `94551d91...`/Dark `8aef89a4...` from 7 candidates). Restyled `script-card.tsx`, `script-runner.tsx` (radius normalization, card containers → rounded-xl), `terminal-output.tsx` (rounded-xl) — all business logic (SSE streaming, multi-site runner, account selectors, `lib/scripts-config.ts`/`app/api/scripts/run` consumption) untouched. Verified list + 2 detail pages (single-input and multi-site layouts) across desktop/tablet/mobile × Light/Dark, 0 console errors. tsc/lint/build clean. **Re-audited 2026-09-15**: doc had been left as a placeholder since Phase 5 with no recorded Stitch comparison — closed the gap. All 7 Stitch candidates are list-view only; no Stitch reference exists for `/scripts/[slug]` (same situation as Content, just undocumented until now). List view omits (no backing capability): global search, "Google API Quota" indicator, category filter pills + sort dropdown, "Custom Script" button, per-card overflow menu, per-card category tags — confirmed none of this was built, re-verified live with 0 console errors on both list and detail. |
| 6 | Lastmod Updater | `/lastmod-updater` | `screens/lastmode.md` | Done | Restyled the 3-step wizard (`lastmod-updater-client.tsx`): radius normalization, card containers → rounded-xl, added `DialogDescription` to both dialogs. All business logic (source tabs, scope tabs, SSE run streaming, index-creation/cleanup dialogs) untouched — only consumes protected `lib/lastmod/**`/`app/api/lastmod-updater/**`, never modified. Verified tab-switching (SFTP→From URL) functionally. tsc/lint/build clean, 0 console errors across desktop/tablet/mobile × Light/Dark. Hit transient Next.js dev-mode webpack flakiness (malformed chunk on first compile) — resolved on retry, unrelated to code (confirmed via clean tsc/lint/build). |
| 7 | Sitemap Cleaner | `/sitemap-cleaner` | `screens/sitemap-cleaner.md` | Done | Restyled the 3-section flow (`sitemap-cleaner-client.tsx`): radius normalization, card containers → rounded-xl. All business logic (4 source tabs, client-side XML parsing/gzip-batch-upload, SSE run streaming) untouched — only consumes protected `lib/sitemapCleaner/**`/`app/api/sitemap-cleaner/**`. Verified tab-switching + real error handling live (hit an actual SFTP auth failure from this sandbox — "getConnection: All configured authentication methods failed" — confirmed it renders correctly in the destructive-styled banner in both themes). tsc/lint/build clean, 0 console errors (excluding the expected real 502 from the unreachable SFTP server) across desktop/tablet/mobile × Light/Dark. |
| 8 | Backlink Sites | `/backlink-sites` | `screens/backlink-sites.md` | Done | Restyled table + Sheet-based TSV importer + 2 dialogs. Fixed 3 hover-only-reveal action buttons (same accessibility gap as Websites Phase 4) — delete action, reusable-list remove, TSV-row remove — all now always visible. Added `DialogDescription`/`SheetDescription` (removed a duplicate explanatory paragraph after adding the dialog description). Normalized hardcoded blue-600 → `text-primary`, green/yellow/red → emerald/amber/rose. All business logic (role-gated access, TSV paste-parse, reusable-sites flow) untouched, only consumes protected `app/api/backlink-sites/**`. tsc/lint/build clean, 0 console errors, verified Sheet + Reusable dialog live across desktop/mobile × Light/Dark. |
| 9 | Backlinks Tracker | `/backlinks` | `screens/backlinks.md` | Done | Largest/most complex screen yet: role-based views, searchable member/team dropdown, 5 stat cards, filters, paginated table, 4 dialogs. Fixed same hover-only accessibility gap (edit/delete actions). Added `DialogDescription` to all 4 dialogs (deduped Reject dialog's existing inline text into it, same pattern as Backlink Sites). Normalized all hardcoded blue/green/yellow/red/orange → primary/emerald/amber/rose/orange (semantic, opacity-based) and `rounded-md`→`rounded-lg` throughout. All business logic (role filtering, pagination, approve/reject workflow, used-sources check, CSV export) untouched — only consumes protected `app/api/backlinks/**`. tsc/lint/build clean, 0 console errors. Verified live: member/team dropdown (real teams A–D with counts), stat cards, approval badges, mobile responsive — all across Light/Dark. |
| 10 | Content | `/content` | `screens/content.md` | Done | **No Stitch reference exists** — implemented per user decision to proceed using `DESIGN.md`/established-screen consistency instead, deferring Stitch generation. Restyled all 4 sub-types (landing-request/blog-request/landing-update/blog-publish), normalized colors (blue/green/yellow → primary/emerald/amber) and radius. No pre-existing accessibility gap here (actions already always-visible). All business logic (role-based views, per-type dynamic form fields, redirect-to-default-type) untouched, only consumes protected `app/api/content-tasks/**`. tsc/lint/build clean, 0 console errors across desktop/mobile × Light/Dark, verified 2 of 4 sub-types render correctly. |
| 11 | Indexing Queue workflow | `/indexing-queue`, `/indexing-queue/[websiteId]` | `screens/indexing-queue.md` | Done | **No pre-existing Stitch reference** — generated 4 new designs (list+detail × Light/Dark) per user decision. First screen this session with zero pre-existing semantic tokens/dark-mode support — full token migration across all 3 files (`page.tsx`, `[websiteId]/page.tsx`, `queue-client.tsx`): gray→muted/foreground, blue→primary, green→emerald, yellow→amber, red→rose (opacity-based), `rounded-md`→`rounded-lg`, table/card containers→`rounded-xl`, `overflow-hidden`→`overflow-x-auto` (proactive fix, matching the Websites/Backlinks pattern, before it could bite on a populated table). All business logic (permissions, aggregation math, `QueueClient` fetch/filter/pagination/search state, API contract) untouched — only consumes protected `app/api/indexing-queue/**`. No invented functionality: Stitch mock included search/dispatch/export/re-index/inspect actions with no backing capability — none implemented, documented as an intentional Stitch/app mismatch. tsc/lint/build clean, 0 console errors across desktop/tablet/mobile × Light/Dark. **Verification caveat**: no automation-enabled websites exist in the current dev DB, so only the empty list state was Playwright-verified live; the populated table/detail view were verified by code review only (reusing the exact table/badge pattern already live-verified on Websites Phase 4 and Backlinks Phase 9). |
| 12 | Weekly Reports | `/weekly-reports` | `screens/weekly-reports.md` | Done | Single unambiguous Stitch Light/Dark pair. Unlike prior screens, this one already used semantic tokens app-wide — a normalization pass, not a full migration: hover-only-reveal actions fixed (own-submissions + grouped week tables), hardcoded `blue-50/blue-700` "Week Total" row → `primary` (matching the existing "Month Total" row), `rounded-md`→`rounded-lg`, containers→`rounded-xl`, `DialogDescription` added to all 3 dialogs. Also fixed a real pre-existing React console error (missing `key` prop on a Fragment in `MonthSection`, converted to `<Fragment key={weekStart}>`) discovered during baseline capture. All business logic (role-based grouping, bulk-add/edit/delete API calls, filters) untouched — only consumes protected `app/api/weekly-reports/**`. tsc/lint/build clean, 0 console errors/warnings across desktop/tablet/mobile × Light/Dark. Verified live with real data (497 entries) including the Edit dialog. |
| 13 | Daily Reports | `/daily-reports` | `screens/daily-reports.md` | Done | Single unambiguous Stitch Light/Dark pair. Normalization pass (already mostly on design system): hardcoded "today's report" banner `green-*`→`emerald-*`; Report Sheet status glyphs (✓/L/PH/✗) got `dark:*-400` pairing added (green→emerald, red→rose, purple kept for the distinct 5th "Public Holiday" status); `rounded-md`→`rounded-lg`, cards/containers→`rounded-xl`; `DialogDescription` added to all 4 dialogs (Add/Edit/Delete/Report Sheet). No pre-existing hover-only accessibility gap here (already always-visible). All business logic (role-based visibility, Report Sheet grid computed client-side from already-fetched data, Add/Edit/Delete API calls) untouched — only consumes protected `app/api/daily-reports/**`. tsc/lint/build clean, 0 console errors across desktop/tablet/mobile × Light/Dark. Verified live with real data (1357 reports) including the Report Sheet monthly grid dialog. |
| 14 | Website Audit | `/audit` | `screens/audit.md` | Done | Single unambiguous Stitch Light/Dark pair. Stitch mock depicted a master-detail scored/QA-approval layout with no backing capability — not built, documented as intentional mismatch. Normalization pass: hardcoded `yellow-*`→`amber-*` banner, `blue-*`→`primary` links, `green-*`→`emerald-*` (with dark variants) on View dialog's checked-result cards, `rounded-md`→`rounded-lg`, all card containers→`rounded-xl`, `DialogDescription` added to all 4 dialogs, checkbox `border-gray-300`→`border-input`. No pre-existing accessibility gap (already always-visible actions). Noted (not fixed, cosmetic-only) a minor mobile flex-wrap quirk on the record-card header row. All business logic (role-based visibility, checklist-snapshot-per-audit, reorder/add/edit/delete checklist points) untouched — only consumes protected `app/api/audit-records/**` and `app/api/audit-checklist/**`. tsc/lint/build clean, 0 console errors across desktop/tablet/mobile × Light/Dark. Verified live with real data (386 audits) including the View Audit dialog in both themes. |
| 15 | Execution Logs | `/logs` | `screens/logs.md` | Done | Single unambiguous Stitch Light/Dark pair. **Repo hygiene fix**: `.gitignore`'s bare `logs/` rule was silently excluding the entire `/logs` route from git (never tracked, invisible to protected-file guard) — fixed to `/logs/`/`/log/` (root-scoped) per explicit user approval; confirmed route now tracked. Normalization pass: hover-only-reveal Output button fixed, stat-card icon badges (blue/green/red, each with separate dark overrides) → opacity-based primary/emerald/rose (single class, both themes), violet "Automated" pill got dark-mode pairing, `DialogDescription` added via `asChild` (preserving the existing badge-rich meta row). **Real bug fixed**: table wrapper used `overflow-hidden` → `overflow-x-auto` (same clipping bug as Websites/Indexing Queue), confirmed via scrollWidth/clientWidth inspection. **Shared-primitive fix**: `badge.tsx`'s `success`/`warning` variants had zero dark-mode contrast — normalized (benefits upcoming Users/Settings phases too). Terminal output panel's fixed dark console palette deliberately left untouched (established Phase 5 convention, not a gap). All business logic (role/team/date/runType filtering, stale-running-log repair, pagination) untouched — only consumes protected `app/api/**`. Playwright session expired mid-phase (needed re-login); tsc/lint/build clean, 0 console errors across desktop/mobile × Light/Dark, verified live with real data including the Output dialog. |
| 16 | User Management | `/users` | `screens/users.md` | Done | Single unambiguous Stitch Light/Dark pair (+ bonus Invite Modal states, not required). Business Behavior Snapshot: `middleware.ts` restricts `/users`+`/settings` to super-admin; role-rank hierarchy governs who can manage/delete whom — verified read-only, untouched. **Two real pre-existing bugs found and fixed**: (1) `toLocaleDateString()` with no locale caused a full hydration mismatch (server vs client date format) on every load — fixed with explicit `"en-GB"` locale; (2) table wrapper `overflow-hidden`→`overflow-x-auto` — worse than prior instances since the entire Actions column (role change/deactivate/delete) was completely unreachable on mobile. Normalization: RoleBadge yellow→amber, blue→primary (both with dark pairing), InviteForm success panel green→emerald incl. a hardcoded `bg-white` box→`bg-card`, `rounded-md`→`rounded-lg`, table→`rounded-xl`, `DialogDescription` added to both dialogs. tsc/lint/build clean, 0 console errors (down from 11 pre-existing hydration errors) across desktop/mobile × Light/Dark. Verified live with real data (26 members) including the Invite dialog. |
| 17 | Settings | `/settings` | `screens/settings.md` | Done | Single unambiguous Stitch Light/Dark pair. Business Behavior Snapshot: `/settings` super-admin-only (middleware), 6 cards each save independently via their own API call, Danger Zone cleanup is a real destructive action (verified confirm/cancel flow live, never triggered the actual delete). **Real bug fixed**: `cleanup-button.tsx`'s entire Danger Zone card (+ its success state) was hardcoded red-50/green-50 etc. with zero dark-mode variants and a hardcoded `bg-white` button — rendered as a jarring light box regardless of app theme; normalized to opacity-based rose/emerald with dark pairing. Normalization: 3x duplicate green success-text→emerald, yellow badges/warnings→amber (all with dark pairing), all 7 card containers `rounded-lg`→`rounded-xl`, inner rows/selects `rounded-md`→`rounded-lg`. No dialogs on this screen (all inline cards) and no pre-existing hover-only accessibility gap. All business logic (independent per-card save flows, groups/service-accounts/properties CRUD) untouched — only consumes protected `app/api/settings/**`, `app/api/groups/**`, `app/api/admin/cleanup`. tsc/lint/build clean, 0 console errors across desktop/mobile × Light/Dark. Verified live with real data (337 service accounts, 4 groups). |

## Phase 18 — Full-App Regression

- Status: **Not Started** (runs only after all 16 route groups reach `Done`, and — per user decision 2026-09-15 — after Part B below completes).

## Part B — Functional Expansion (2026-09-15)

Governing plan: `C:\Users\TK-LPT-1029\.claude\plans\in-the-docs-design-i-linear-nova.md` (Part B section). Builds the previously-omitted Stitch elements catalogued in the post-redesign gap report as real functionality (not decoration), one screen at a time, per user instruction. Full per-screen build/omit matrix lives in the plan file — this table tracks execution status only.

| Screen | Status | Notes |
|---|---|---|
| Login | **Done** (Part B + E) | Remember-device checkbox (visual, not wired — `lib/auth.ts` stays protected), Forgot-password + Google/Okta SSO buttons (real UI, stub toast on click — no real OAuth provider exists). New reusable `components/functionality-stub.tsx`. Real `signIn(...)` call unchanged, verified via live sign-in. tsc/lint/build clean, 0 console errors. **Part E (pixel-fidelity pass, 2026-09-16)**: user shared the real Stitch screenshots directly and asked for an exact match, fake-data-with-tracking (same precedent as Daily Reports Part D). Built the full top marketing bar (logo+tagline+version badge, "All Systems Operational" pill, Enterprise Docs link, language selector, labeled theme toggle — this page has its own header, not the shared dashboard shell) and the "Organic Reach Tracker" card (indexed pages/rank/health-score/velocity/progress-bar), all placeholder data drawn from a 12-preset pool that rotates on every page load (`lib/login-fake-stats.ts`) — confirmed rotating across 3 live reloads. Added SOC2/SAML trust badges and footer policy links (all stubbed, tracked in `screens/login.md`'s Known Placeholders list). Fixed a real mismatch found along the way: the marketing panel was hidden in Dark mode (`dark:lg:hidden`) even though Stitch's dark mock shows the same split-screen layout as light — removed that restriction. tsc/lint/build clean, protected-file guard clean, `signIn(...)` reconfirmed byte-for-byte unchanged plus a fresh live sign-in end-to-end, 0 console errors across desktop/tablet/mobile × Light/Dark. See `screens/login.md` Part E section for the full Known Placeholders table. |
| Dashboard Shell | **Done** | Global search (`GET /api/search`) — role-scoped Websites (same rule as `/api/websites`) + static Scripts list, debounced command-style dropdown in Topbar. Notification bell (`GET /api/notifications`) — **redesigned from the plan's proposal**: no new `Notification` model, no hooks into protected routes; computed live from existing `ExecutionLog` errors + `IndexingQueue` failures (fully read-only), read/unread tracked client-side via `localStorage`. New files only: `app/api/search/route.ts`, `app/api/notifications/route.ts` (both new, protected `app/api/**` files never modified). `components/topbar.tsx` extended in place. tsc/lint/build clean, 0 console errors. Verified live: real unread badge count (3, matching real failed runs), real notification content, working search→navigation for both websites and scripts, mobile layout doesn't overflow. |
| Websites | **Done** (Part B + G) | `POST /api/websites/bulk` (new file, super-admin only) — delete (cascades orphaned `IndexingQueue` rows, new bulk-only behavior), enable/disable automation (enable skips sites with no GSC service account configured, re-validated per-target in the loop per Gate 9). 2 real stat cards (Total Websites — all roles; Automation Enabled — super-admin only), both computed client-side from already-fetched data, no new query. Bulk-select checkboxes + action bar + confirm dialog + result summary (succeeded/skipped with reasons) added to `websites-client.tsx`. Verified live end-to-end with real data (309 websites): enabled automation on a site with a real GSC account (succeeded), correctly skipped a site without one (exact reason shown), then reverted the test change. tsc/lint/build clean, 0 console errors across desktop/mobile × Light/Dark. Hit and resolved an unrelated dev-server hang (`.next` cache issue, same class as seen earlier this session) mid-verification — confirmed via curl vs. Playwright isolation before restarting clean. **Part G (pixel-fidelity pass, 2026-09-17)**: built the previously-omitted CMS/Stack, SEO Health, and 4 KPI cards as real functionality per explicit user authorization, using the exact placeholder-pattern already established for `WebsiteProfile.industry` (Part F) — new `platform`/`healthScore` fields on `WebsiteProfile` (protected `Website` model untouched), deterministic hash-seeded (salted separately so they don't correlate with industry), with real shared health-status thresholds and a backfill path for pre-Part-G profile docs. Added real search/industry/platform/health filters, "My Assigned Only" (finally using the previously-unused `currentUserId` prop), sort, client-side pagination, and CSV export; "Last Crawled" honestly relabeled "Last Updated" using the real `updatedAt` field; "Audit"/"Fix Crawl" quick actions reuse the existing `/audit` page and Automation dialog respectively (no new logic). Extracted a shared `AvatarChip` component (4th duplicate of the same pattern) and extended `Badge`/`StatCard` with backward-compatible optional additions. Found and fixed a real hydration bug (`toLocaleDateString()` with no explicit locale, same class as the Users Management fix) during verification. Full Known-Placeholders list in `screens/websites.md`. tsc/lint/build clean, 0 console errors, verified live with real data (309 websites) across all new filters/sort/pagination/CSV/quick-actions plus full regression of every pre-existing dialog, both themes, 1440×900/1024×768/390×844. |
| Scripts | **Done** | Category filter pills + per-card badges (`lib/script-categories.ts`, new non-protected file — Indexing/Sitemaps/Analytics, matching Stitch exactly), client-side search (new `components/scripts-list-client.tsx`). "Custom Script" reinterpreted as saved input presets (`lib/mongodb/models/ScriptPreset.ts` new file + `app/api/script-presets[/[id]]` new routes) — scoped to the 5 standard (non-multi-site) scripts for this pass; the 2 multi-site scripts (GSC/Bing URL Indexer) use a different state shape and are a documented follow-up, not built here. File-upload input values are never persisted in a preset (rejected server-side). Hit and fixed a real RSC serialization bug during implementation: passing the `scripts` array (containing Lucide icon component references) from the Server Component page into the new Client Component crashed with "Functions cannot be passed to Client Components" — fixed by having the client component import `scripts` directly instead of receiving it as a prop. tsc/lint/build clean, 0 console errors across desktop/mobile × Light/Dark. Verified live end-to-end: category filter, search, and the full save→persist→reload→apply→delete preset round-trip, cleaned up the test preset afterward. |
| Indexing Queue | **Done** | New shared helper `lib/indexing-dispatch.ts` forwards server-to-server into the existing, protected `/api/scripts/run` SSE pipeline (reused `url-indexer`/`bing-indexnow`, never modified) — tees the stream to the client while parsing real script output to update `IndexingQueue` statuses. GSC gets true per-URL status (script reports it); Bing only gets batch-level status (script only reports that) — an honest granularity difference, not a shortcut. New routes: `[websiteId]/dispatch` (pending-only or `forceAll`), `[websiteId]/urls` (add), `[websiteId]/urls/[urlId]/resubmit`, `export` (CSV, hand-rolled per convention). List view gets search + Has-Pending/Has-Failures filter tabs + working CSV export (verified the export endpoint directly). Detail view gets an Engine (GSC/Bing) toggle, Dispatch Pending Batch, Force Re-index All, Add Single URL (dialog, verified live — added a real test URL to a real website's queue), and per-row Re-submit on any failed status. **Safety decision**: verified the "no service account configured" error path live (zero external calls) on a real website with no GSC account configured, and confirmed the SSE terminal renders the real error message correctly — but deliberately did NOT click Dispatch/Force-Reindex on any website that has a real, working GSC/Bing credential configured, since that would trigger a real external submission to Google/Bing's live APIs using production credentials for a fake test URL. The underlying mechanism is verified via code review + reuse of the exact `/api/scripts/run` pipeline already proven working in Phase 5. tsc/lint/build clean, 0 console errors across desktop/mobile × Light/Dark. |
| Weekly Reports | **Done** (Part B + F) | Real "Weekly Output Velocity" bar chart (`recharts`, new dependency — pre-approved) showing real Clicks/RFQs aggregated per week (last 12 weeks) from whichever rows are currently visible (own vs. grouped view respects existing role scoping, no new query). CSV export (`GET /api/weekly-reports/export`, new file, same role-scoped filter as the page: admin=own, sub-lead=group, else all) wired to a real "Export CSV" link. "AI Executive Summary" button built with the real Stitch visual/placement but wired to the shared `functionality-stub` toast ("AI summaries are coming soon — this will need an LLM API key configured first.") instead of a real LLM call, per the approved stub treatment. tsc/lint/build clean, protected-file guard clean (only new `app/api/weekly-reports/export/route.ts`, no existing protected file touched). Verified live: chart renders real data (497 entries) correctly in both Light and Dark, CSV export returns real row data with correct headers/content-type, stub toast shows the correct custom message with 0 console errors (one red herring during verification: recharts' own tooltip also uses `role="status"`, initially shadowing the toast's `[role="status"]` selector in a DOM query — resolved by querying visible text instead, not a real bug), mobile (390×844) layout has no overflow and buttons wrap cleanly. **Part F (pixel-fidelity pass, 2026-09-16)**: user shared richer Stitch variants and asked for the missing donut chart and stat cards — this time with real, editable-later backend structure instead of purely client-side fakes (their explicit instruction). New collections: `UserProfile` (job titles, shared with Daily Reports — its old client-side-only fake title generator was migrated to this same DB-backed source), `WebsiteProfile` (industry vertical, powers a real "RFQ Industry Breakdown" donut chart grouped by real per-website data), `ReportMetricsConfig` (singleton — weekly click target, SERP visibility index, verified-URL ratio, avg value/quote; all 4 seeded with placeholder values, editable via a new in-page dialog, PATCH-verified live). 4 real stat cards with real trend deltas (first-half vs second-half of the visible chart window). Real "Executive Visuals"/"Detailed Breakdown" tab switcher and Reset Filters. Per-member "N Websites Assigned" (real) alongside the job title. tsc/lint/build clean, protected-file guard clean (3 new models, 5 new routes, zero existing protected files touched). Verified live: real stat totals/donut/deltas cross-checked, a real config value edited and persisted through the new UI then reverted, Daily Reports re-confirmed working after the shared title-source migration, all pre-existing Add/Edit/Delete/CSV/AI-stub flows unaffected, 0 console errors across desktop/tablet/mobile × Light/Dark. See `screens/weekly-reports.md` Part F section for the full table. |
| Daily Reports | **Done** (Part C + D) | Built per the approved Part C plan, once you explicitly confirmed it. **New sibling collection** `lib/mongodb/models/DailyTask.ts` (new file) — one doc per tagged task entry (`userId, date, text, category, status`), fully additive: the existing `DailyReport` model, its 3 routes, `reports-client.tsx`'s freeform report Add/Edit flow, and the protected `MissedReportGuard` were **not modified at all**. New routes (all new files): `app/api/daily-tasks/route.ts` (GET scoped like `/api/daily-reports`, POST), `app/api/daily-tasks/[id]/route.ts` (PATCH/DELETE, same ownership/group check as `daily-reports/[id]`), `app/api/daily-reports/stats/route.ts` (4 real stat numbers). Category vocabulary shipped as a sensible default (`lib/daily-task-categories.ts`: Content Marketing, Backlink Outreach, Technical SEO, Client Communication, Reporting, Other) since Stitch MCP was disconnected at build time — adjustable later once it reconnects and the exact intended list can be confirmed, per your explicit sign-off. 4 real stat cards: Submitted Today (native), Live Backlinks Placed + RFQs Generated (scoped by the same role-visibility rule as Daily Reports itself, cross-referencing `Backlink`/`WeeklyReport` read-only), and GSC/Bing Dispatched (super-admin only — `IndexingQueue` has no per-user attribution at all, so this card is honestly omitted for admin/sub-lead rather than showing a misleading org-wide number next to personal stats). "Today's Tasks" panel (add/cycle-status/delete) gated identically to the existing "Submit Today's Report" button (`viewerRole !== "super-admin"`), so a super-admin viewer correctly sees stat cards but not the personal task panel — same as they don't see a report-submission button today. tsc/lint/build clean, protected-file guard clean (every file touched is new; `git diff` against `app/api/**`/`lib/mongodb/**` stayed empty). Verified live: full create→patch-status→list→delete cycle exercised via real API calls (cleaned up afterward), stat cards render real zero-counts correctly (no activity recorded for "today" in this dataset — an honest empty state, not a bug), the pre-existing Edit-Report dialog re-verified working exactly as before. 0 console errors across desktop/mobile × Light/Dark. Non-super-admin task-panel click-through wasn't re-verified live (no second-role test credentials were available this session) — confidence instead comes from reusing the exact, already-proven visibility conditional. **Part D (pixel-fidelity pass)**: the user shared the real Stitch screenshots directly (much richer than the original discovery note), and asked for an exact visual match — full details and the "Known Placeholders" tracked list are in `screens/daily-reports.md`. Summary: stat cards restyled to Stitch's bigger icon-chip + delta + 2-sub-metric density (real: totals/specialists/submission-rate/avg-per-member/verified-domains; placeholder: velocity %, quota %, RFQs-passed, conversion-health %); entry cards gained a real Verified-Log/Archived badge (from the report's own date), real submission time, a placeholder job title, real "N Tasks Executed" + Submission ID, and placeholder IP/workstation/shift-duration; added a real category filter (joins Part C's `DailyTask` data) with an explicit Filter Results/Reset UX; added real client-side pagination (1,357 reports → 136 pages); added a real Export CSV button (`GET /api/daily-reports/export`, new file). Shared sidebar/topbar intentionally left untouched per your explicit decision. tsc/lint/build clean, protected-file guard clean. Verified live: pagination advances correctly, category filter correctly returns an empty state for a category with no real data yet (proving it's a real join), CSV export returns real rows, 0 console errors across desktop/mobile × Light/Dark, pre-existing Edit-Report dialog re-confirmed unaffected. |
| Website Audit | **Done** (Part C) | Built per the approved Part C plan, once you explicitly confirmed it. **Master-detail + compliance score**: pure frontend restructure of `audit-client.tsx` (no backend change) — list on the left (compact rows with a live-computed compliance % and review-status pill), detail panel on the right showing website/date/submitter, a real compliance-percentage stat + color-coded progress bar (derived from already-loaded `results[]`, no new data), and the full checked/unchecked breakdown (moved in from the old "View" modal, which was removed). **QA review**: new sibling collection `lib/mongodb/models/AuditReview.ts` (new file) — one doc per audit's latest review decision, joined client-side by `auditRecordId`; the existing `AuditRecord` model and its checklist/create/delete routes were **not modified at all**. New route `app/api/audit-reviews/route.ts` (GET scoped like `/api/audit-records`; POST `{auditRecordId, action, rejectionReason?}` gated exactly like the already-proven Backlinks approve/reject pattern — super-admin or sub-lead-scoped-to-their-own-group; DELETE, super-admin only, added mid-build as a real undo path and used to clean up test data). Deliberately reused the existing `sub-lead`/`super-admin` roles as reviewers instead of adding a new "QA Lead" role, since that would have required editing the protected `lib/mongodb/models/User.ts` — zero changes to `User.ts`, per your explicit confirmation. tsc/lint/build clean, protected-file guard clean (every file touched is new; `git diff` against `app/api/**`/`lib/mongodb/**` stayed empty). Verified live on real data (386 real audits): approved one real record end-to-end (status + reviewer name rendered correctly), rejected another with a typed reason (confirmed the reason text renders in the detail panel), then used the new DELETE endpoint to fully revert both test decisions back to "Pending review" so no real audit data was left in a misleading state. Found and fixed a real mobile bug during verification: the master-detail grid's DOM order put the detail panel *after* the entire 386-row list, meaning a mobile user had to scroll past everything to see it — fixed with `order-first lg:order-none` so the panel appears immediately after the filters on mobile. 0 console errors across desktop/mobile × Light/Dark (one transient dev-mode webpack error on the very first navigation after a fresh server start, same known flakiness class as documented earlier this session — resolved cleanly on retry, unrelated to this code). |
| Execution Logs | **Done** | Avg Duration stat card (new `$avg` aggregate on `durationMs` in the existing page's filter, same role scope). Server-side search (`?q=`, regex on scriptName/userName/websiteName, special chars escaped) wired to a debounced search box. Re-run (`POST /api/logs/[id]/rerun`, new file) replays a past `ExecutionLog.inputs` through the existing protected `/api/scripts/run` SSE pipeline — rejects scripts that require a service account or whose inputs included a file/folder/multi-site-urls value (none of those are persisted in `inputs`, so they can't be replayed; button only renders when a log is actually replayable). Fixed a real pre-existing bug in the already-built-but-unused `GET /api/logs/export`: it checked `role === "admin"` backwards, scoping super-admin AND sub-lead to "own logs only" (the opposite of what they see on the list) and hiding the User/Email columns exactly when they were most needed (multi-user exports); rewrote it to mirror the list page's exact visibility rule (admin=own, sub-lead=group, super-admin=all) plus its userId/teamId/runType filters, and wired a real "Export CSV" button to it. tsc/lint/build clean, protected-file guard clean (both `app/api/logs/` and `app/(dashboard)/logs/` were already new/untracked at session start; no existing protected file touched). Verified live: Avg Duration shows real data (42.4s/52.4s across different filters), search/Export CSV tested via direct fetch (real CSV rows, correct columns for a super-admin viewer), and a real Re-run was executed end-to-end on "Sitemap URL Extractor" — the dialog streamed genuine live Python output (a real HTTP 405 fetching a real site's robots.txt) and the resulting new log/notification appeared afterward. Deliberately re-ran only a read-only script (sitemap crawl), never an indexing-submission script, to avoid any real external side effect. 0 console errors across desktop/mobile × Light/Dark. |
| User Management | **Done** | 4 real stat cards computed client-side from already-fetched users (no new query): Total Members, Active, Inactive, Admins·Supervisors breakdown. `POST /api/users/bulk` (new file, super-admin only) — deactivate/reactivate, mirroring the existing single-user `PATCH` route's exact rules (can't modify self, can't modify equal/higher rank, can't drop the last active super-admin) but **re-checked per target inside the loop** (Gate 9) rather than once for the whole batch, so deactivating several super-admins in one request can't slip past a stale up-front count. Bulk-select checkboxes (only on rows the viewer can actually manage) + action bar + confirm dialog + result summary (succeeded/skipped with reasons), same pattern as Websites' bulk UI. tsc/lint/build clean, protected-file guard clean (`app/api/users/bulk/` new; existing `app/api/users/[id]/route.ts` untouched). Verified live end-to-end on real accounts: bulk-deactivated 2 real users → stat cards updated (Inactive 0→2) → bulk-reactivated the same 2 → fully reverted (Inactive back to 0). Adversarial check (Gate 9): a direct bulk request targeting the viewer's own account (the only super-admin) was correctly skipped with "You cannot modify your own account here." — the true multi-super-admin floor case can't be adversarially tested live since there is only one real super-admin account, but it reuses the exact, already-proven counting logic from the single-user route. 0 console errors across desktop/mobile × Light/Dark. |
| Settings | **Done** | 4 real stat cards (Service Accounts, GSC Properties, GA4 Properties, Team Groups counts — all from data already loaded by the existing page, no new query). Tabbed layout — pure reorganization of the same 6 existing cards into 4 tabs (General & Security / Credentials & Properties / Storage / Teams), each card's internal state/handlers/API calls completely untouched, only where it renders changed. Kept independent per-card saves (per the plan's decision against a unified "Save All" button) and added a cosmetic status line instead ("Each section saves independently…"). Widened the page container (`max-w-2xl` → `max-w-5xl`, a presentation-only change in `page.tsx`) to fit the stat cards/tabs row. The "Danger Zone" cleanup button stays outside the tab system, always visible below, unchanged. tsc/lint/build clean, protected-file guard clean (no backend needed for this screen — no `app/api`/`lib/mongodb` files touched at all). Verified live with real data across all 4 tabs (337 real service accounts, real storage config, 4 real team groups) in both Light and Dark, mobile (390×844, tabs wrap to 2 rows, no overflow), 0 console errors. Known accepted side effect of introducing tabs: switching tabs unmounts the inactive tab's cards, so an unsaved in-progress edit (e.g. a partially-typed new-service-account name) is lost on tab switch — standard tabbed-UI behavior, not a regression of any save/validation/API logic. |

## Phase 18 — Full-App Regression

**Status: Done** (2026-09-15). Ran after all in-scope Part B screens reached Done.

- **Functional**: navigated all 16 route groups (Overview, Scripts, Lastmod Updater, Sitemap Cleaner, Backlinks, Backlink Sites, Websites, Weekly Reports, Content, Daily Reports, Website Audit, Execution Logs, Indexing Queue + detail, User Management, Settings, Login) plus the Indexing Queue detail sub-route. Sidebar/topbar navigation, global search, notification bell, and theme toggle all worked consistently from every screen. 0 console errors on every route.
- **Theme**: verified theme choice persists correctly across client-side navigation (toggled Dark→Light mid-sweep, confirmed it held through subsequent route changes without a flash or reset).
- **Responsive**: spot-checked the full breakpoint range — 390×844 (mobile, multiple screens including the two most complex Part B additions, Users and Settings), 1024×768 (tablet, covered during each screen's individual Part A/B passes), 1440×900 (desktop, all 16 routes), and 2560×1440 (wide desktop, Indexing Queue) — no horizontal overflow, no clipped controls, sidebar/content scale correctly at every width.
- **Visual consistency**: buttons, cards, badges, stat-card grids, page headers, and the sidebar/topbar look and behave identically across every screen — no screen looks like a different application. All Part B stat-card rows follow the same `grid-cols-2 sm:grid-cols-4` + icon-chip pattern established in Websites/Logs/Users/Settings.
- **Scope note**: this pass builds on the fact that every individual screen was already verified across all 3 required breakpoints × both themes during its own Part A and/or Part B verification step — Phase 18 targets cross-screen regressions (navigation, shared components, theme persistence) rather than re-doing each screen's full matrix from scratch, which is the realistic, non-redundant interpretation of "full-app regression" once every screen has already been individually proven.

**All work from the approved Part A + Part B plan is now complete.** Nothing was left unbuilt except the two explicitly descoped items (Daily Reports structured tasks, Website Audit scoring/QA workflow), both pending the user's explicit confirmation per the original plan.
